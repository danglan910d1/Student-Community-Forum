export const PAGINATION_DATA = {
  currentPage: 1,
  totalPages: 120,
  hasNextPage: true,
  hasPrevPage: false,
  // Sau này khi gọi API, bạn chỉ cần update object này
};

export const PAGINATION_CONFIG = {
  postsPerPage: 5,
  totalPosts: 10, // Tổng 10 bài đã làm ở trên
};
